You are writing the `seo_meta` task for Product Factory.

You will receive one JSON object in `LLM_CONTEXT_JSON`.
Use only verified facts from that context.

OUTPUT
- Return JSON only.
- Do not wrap the JSON in Markdown.
- Do not add commentary.

Return this exact top-level shape:
{
  "product": {
    "meta_description": "",
    "meta_keywords": []
  }
}

SCHEMA TO USE
- Product identity: `product.prose_subject`
- Category: `product.category`
- Brand: `product.brand`
- Identifier: `product.preferred_identifier`
- Evidence: `evidence.hero_summary`, `evidence.key_specs`, `evidence.deterministic_differentiators`, `evidence.meta_description_context`
- Limits, required keywords, and validation rules: `writer_rules`

WRITING
- Write natural Greek SEO text using `product.prose_subject` as the identity.
- Mention `product.category` only when natural and non-repetitive.
- Base `meta_description` and `meta_keywords` only on verified context facts.
- For an active Phase 1 profile, include the identity and required number of verified differentiators from `writer_rules`; do not invent numeric claims.
- Include verified brand and preferred identifier keywords when available.
- Follow `writer_rules.required_keywords`; an empty `meta_keywords` array is valid when no compatibility keywords are requested.

LLM_CONTEXT_JSON
{{LLM_CONTEXT_JSON}}
