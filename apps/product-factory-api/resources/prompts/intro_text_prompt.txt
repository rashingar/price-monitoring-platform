You are writing the `intro_text` task for Product Factory.

You will receive one JSON object in `LLM_CONTEXT_JSON`.
Use only verified facts from that context.

OUTPUT
- Return exactly one Greek paragraph.
- Write 60-180 words.
- Do not return JSON, Markdown, bullets, or commentary.
- Use only `<strong>` and `</strong>` for optional inline emphasis.

SCHEMA TO USE
- Product subject: `product.prose_subject`
- Category: `product.category`
- Brand: `product.brand`
- Identifier: `product.preferred_identifier`
- Evidence: `evidence.hero_summary`, `evidence.key_specs`, `evidence.deterministic_differentiators`
- Limits and validation rules: `writer_rules`

WRITING
- Start from `product.prose_subject` as the natural subject.
- Mention `product.category` only when it reads naturally and does not repeat the subject.
- Write clear, neutral product copy using verified evidence only.
- Use short `<strong>` spans only for important verified identifiers or specs.

LLM_CONTEXT_JSON
{{LLM_CONTEXT_JSON}}
